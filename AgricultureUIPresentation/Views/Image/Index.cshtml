@using EntityLayer.Concrete
@model List<Image>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}
<div>
    <br />
    <center>
        <h1>Görsel Listesi</h1>
    </center>
    <br />

    <table class="table table-bordered table-hover table-secondary">

        <tr>

            <th style="text-align:center">Başlık</th>
            <th style="text-align:center">Açıklama</th>
            <th style="text-align:center">Sil</th>
            <th style="text-align:center">Düzenle</th>

        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td style="text-align:center">@item.Title</td>
                <td style="text-align:center">@item.Description</td>
                <td style="text-align:center"><a href="/Image/DeleteImage/@item.ImageID" class="btn btn-danger">Sil</a></td>
                <td style="text-align:center"><a href="/Image/EditImage/@item.ImageID" class="btn btn-warning">Düzenle</a></td>
            </tr>
        }
    </table>
    <br />
    <a href="/Image/AddImage" class="btn btn-outline-primary">Yeni Görsel Girişi</a>
</div>
